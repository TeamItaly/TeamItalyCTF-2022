window.innerWidth < 768 &&
  [].slice.call(document.querySelectorAll('[data-bss-disabled-mobile]')).forEach(function (e) {
    e.classList.remove('animated'),
      e.removeAttribute('data-bss-hover-animate'),
      e.removeAttribute('data-aos');
  }),
  document.addEventListener(
    'DOMContentLoaded',
    function () {
      AOS.init();
    },
    !1
  ),
  (function () {
    'use strict';
    var e = document.querySelector('#mainNav');
    if (e) {
      var t = function () {
        (void 0 !== window.pageYOffset
          ? window.pageYOffset
          : (document.documentElement || document.body.parentNode || document.body).scrollTop) > 100
          ? e.classList.add('navbar-shrink')
          : e.classList.remove('navbar-shrink');
      };
      t(), document.addEventListener('scroll', t);
    }
    document.getElementsByClassName('popup-gallery').length > 0 &&
      baguetteBox.run('.popup-gallery', { animation: 'slideIn' }),
      (function () {
        if ('requestAnimationFrame' in window && !/Mobile|Android/.test(navigator.userAgent)) {
          var e = document.querySelectorAll('[data-bss-parallax]');
          if (e.length) {
            var t,
              n = [];
            window.addEventListener('scroll', a), window.addEventListener('resize', a), a();
          }
        }
        function a() {
          n.length = 0;
          for (var a = 0; a < e.length; a++) {
            var i = e[a].getBoundingClientRect(),
              r = parseFloat(e[a].getAttribute('data-bss-parallax-speed'), 10) || 0.5;
            i.bottom > 0 && i.top < window.innerHeight && n.push({ speed: r, node: e[a] });
          }
          cancelAnimationFrame(t), n.length && (t = requestAnimationFrame(o));
        }
        function o() {
          for (var e = 0; e < n.length; e++) {
            var t = n[e].node,
              a = n[e].speed;
            t.style.transform = 'translate3d(0, ' + -window.scrollY * a + 'px, 0)';
          }
        }
      })();
  })();
